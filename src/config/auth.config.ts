/**
 * Auth page configuration.
 * Set signInLeftImageSrc when you have the left-panel image asset.
 */
export const authConfig = {
  /** Left panel image for sign-in page. Set to your asset path when ready. */
  signInLeftImageSrc: null as string | null,
};
